# Database — Render sets this automatically via the PostgreSQL addon
# Accepts postgresql://, postgres://, or postgresql+asyncpg:// formats
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/yesveri_election

# Redis — optional, app works without it
REDIS_URL=redis://localhost:6379/0

# CORS — JSON list of allowed frontend origins
# Add your Cloudflare Pages URL here after deployment
CORS_ORIGINS=["http://localhost:5173","http://localhost:8000"]

# Debug mode
DEBUG=false
